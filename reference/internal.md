# Internal

Internal functions.

## Usage

``` r
# S3 method for class 'weeknumber'
vec_arith(op, x, y, ...)

# S3 method for class 'weeknumber'
vec_cast(x, to, ...)

# S3 method for class 'weeknumber'
vec_ptype2(x, y, ...)
```
