# Week number class

Constructor, helper and test functions for the week number class.

## Usage

``` r
new_weeknumber(x = double())

weeknumber(x = double())

is_weeknumber(x)
```

## Arguments

- x:

  An object.
